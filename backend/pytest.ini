[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Live tests hit third-party servers. Opt in with: pytest -m live
addopts = -q --strict-markers -m "not live"

markers =
    live: hits real platform servers; excluded from the default run
